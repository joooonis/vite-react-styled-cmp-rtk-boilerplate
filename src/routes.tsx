import { RouteObject } from 'react-router-dom';

import Layout from './components/common/Layout/Layout';

const router: RouteObject[] = [{ path: '/', element: <Layout /> }];

export default router;
